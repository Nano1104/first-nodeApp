paths:
  /api/views/products:
    get:
      summary: Renderiza la vista de productos
      description: Para acceder a esta vista el usuario debe estar logueado y tener un JWT valido. Cuando renderiza la vista envia el los productos que haya creados y el cart del usuario como parametros 
      tags:
        - Views
      security:
        - JWTTokenCookie: []
      responses:
        "200":
          description: Renderiza la vista de productos.
        "500":
          description: Mensaje de error al intentar renderizar los productos
  /api/carts/{cid}:
    get:
      summary:
      description:
      tags:
        - Views
      parameters:

  








components:
  securitySchema: 
      JWTTokenCookie:
        type: apiKey
        in: cookie
        name: token